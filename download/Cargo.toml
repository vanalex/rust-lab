[package]
name = "download"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.11", features = ["blocking"] }
anyhow = "1.0"
tokio = { version = "1.47.1", features = ["rt", "rt-multi-thread", "macros", "fs"] }
